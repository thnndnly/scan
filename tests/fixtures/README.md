# Test Fixtures

Place sample card images here for use in integration tests.

Expected files (not tracked in git):
- `sample_card.jpg` — a single MTG card photograph
- `multi_card.jpg` — a photo containing several cards

These files are optional; unit tests use synthetic images generated at test time.
